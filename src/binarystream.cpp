#include "filestream.h"
#include <string>
#include <memory> 

std::unique_ptr<mapbits>** BinaryStream::loadMap(std::string filename) {
    
    mapbits** arr = new mapbits*[4];

    return arr;
}

void BinaryStream::writeMap(std::string filename, mapbits** arr) {
    
}

